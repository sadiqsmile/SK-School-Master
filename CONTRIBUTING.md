# Contributing

Thanks for helping improve SK School Master.

## Quick start
1. Create a branch from `dev`
2. Make your changes
3. Run:
   - `flutter analyze`
   - `flutter test`
4. Open a PR into `dev`

## Notes
- Keep Firebase reads/writes efficient (pagination, limits, derived counters).
- Avoid committing secrets (service accounts, keystores, API keys).
