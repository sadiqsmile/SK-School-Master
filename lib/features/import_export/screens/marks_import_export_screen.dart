import 'package:flutter/material.dart';

class MarksImportExportScreen extends StatelessWidget {
  const MarksImportExportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Marks')),
      body: const Center(
        child: Text('Marks Import Export Screen'),
      ),
    );
  }
}