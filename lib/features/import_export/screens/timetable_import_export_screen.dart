import 'package:flutter/material.dart';

class TimetableImportExportScreen extends StatelessWidget {
  const TimetableImportExportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Timetable')),
      body: const Center(
        child: Text('Timetable Import Export Screen'),
      ),
    );
  }
}