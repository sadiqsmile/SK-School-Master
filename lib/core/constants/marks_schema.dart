/// Constants for the canonical marks schema.
///
/// Student marks docs (canonical):
///   subjects.{subjectKey}.{componentKey} = obtained
///
/// Exam max marks (canonical):
///   subjectMaxByComponent.{subjectKey}.{componentKey} = max
const String kTotalComponentKey = 'total';
